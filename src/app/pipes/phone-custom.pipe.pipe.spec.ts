import { PhoneCustomPipePipe } from './phone-custom.pipe.pipe';

describe('PhoneCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
